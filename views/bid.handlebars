<div class="text-center">
  <h2>{{listing_id}}</h2>
  <p>{{{bid_amount}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{car}}</span>
    needed
  </p>
  <p>
    <button class="btn btn-primary">Submit</button>
  </p>
</div>
